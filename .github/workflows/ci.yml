name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: No comments
        run: npm run lint:comments -- --format github

      - name: Lint
        run: npm run lint -- --format github

      - name: Format
        run: npm run format:check

      - name: Unused code
        run: npm run knip -- --reporter github-actions

      - name: Unused code in production
        run: npm run knip:production -- --reporter github-actions

      - name: Type check
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Build
        run: npm run build
