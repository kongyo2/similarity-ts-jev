{
  "name": "@kongyo2/similarity-ts-jev",
  "version": "0.3.0",
  "description": "similarity-ts + fallow duplicate detection for TypeScript, judged by TypeSafe's Jev (via @typesafe-ai/sdk): the pairs worth refactoring, the shape of the refactoring, and a calibration report.",
  "type": "module",
  "license": "MIT",
  "author": "kongyo2",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kongyo2/similarity-ts-jev.git"
  },
  "keywords": [
    "typescript",
    "similarity",
    "duplication",
    "clones",
    "refactoring",
    "jev",
    "typesafe",
    "fallow",
    "calibration",
    "cli"
  ],
  "bin": {
    "similarity-ts-jev": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"test/*.test.ts\"",
    "verify": "node scripts/verify.ts",
    "prepack": "npm run clean && npm run build && npm run typecheck && npm run lint && npm run format:check && npm run knip && npm run knip:production && npm run lint:comments && npm test",
    "record-fixtures": "node scripts/record-fixtures.ts",
    "proxy": "node scripts/proxy.ts",
    "experiment": "node scripts/experiments/run.ts",
    "analyze": "node scripts/experiments/analyze.ts",
    "lint": "oxlint",
    "lint:comments": "ts-comment-scanner --fail-on-comment --ignore node_modules/ --ignore dist/ --ignore .corpora/ .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "knip": "knip",
    "knip:production": "knip --production"
  },
  "dependencies": {
    "@kongyo2/similarity-ts": "^0.7.0",
    "@typesafe-ai/sdk": "^0.6.0",
    "commander": "^15.0.0",
    "fallow": "^3.28.0",
    "ignore": "^7.0.10"
  },
  "devDependencies": {
    "@kongyo2/ts-comment-scanner": "^1.7.0",
    "@types/node": "^26.6.2",
    "knip": "^6.38.0",
    "oxlint": "^1.85.0",
    "prettier": "^3.9.9",
    "typescript": "^7.0.2"
  },
  "homepage": "https://github.com/kongyo2/similarity-ts-jev#readme",
  "bugs": {
    "url": "https://github.com/kongyo2/similarity-ts-jev/issues"
  }
}
